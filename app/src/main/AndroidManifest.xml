<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ashl7developer.autism.feelings">

    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name_long"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivityPager">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ContactSelectActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".SettingActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".PasswordCreateActivity"
            android:label="@string/app_name"
            android:noHistory="true" />
        <activity
            android:name=".PasswordCheckActivity"
            android:label="@string/app_name"
            android:noHistory="true" />
        <activity
            android:name=".WelcomeScreenActivity"
            android:label="@string/app_name"
            android:noHistory="true"/>
        <!-- removes the activity from stack history, so when user presses back, it doesn't go back to this -->

    </application>

</manifest>